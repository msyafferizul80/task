import EisenhowerDashboard from '@/components/task/EisenhowerDashboard';

export default function TaskManagementPage() {
    return (
        <main className="min-h-screen p-8 bg-gray-50">
            <EisenhowerDashboard />
        </main>
    );
}
